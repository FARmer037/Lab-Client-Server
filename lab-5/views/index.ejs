<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100%;
            height: 100vh;
            background-color: rgb(92, 92, 92);
            display: flex;
            justify-content: center;
            align-items: center;
        }

        body, .text-input, .submit-btn {
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }

        .login-cotainer {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: rgba(160, 160, 160, 0.5);
            border-radius: 10px;
            width: 50%;
            height: 50vh;
        }

        .login-cotainer h1 {
            color: rgb(58, 58, 58);
        }
        .login-form {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
            width: 50%;
            height: 50%;
        }
        .text-input {
            height: 40px;
            margin: 10px 0px;
            font-size: 16px;
            padding: 0px 5px;
        }
        .submit-btn {
            height: 40px;
            width: 50%;
            margin: 10px auto;
            font-size: 18px;
            color: white;
            background-color: rgb(78, 78, 78);
            border: none;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <div class="login-cotainer">
        <h1>USER LOGIN</h1>
        <form class="login-form" method='post' action='/'>
            <input class="text-input" type='email' name='email' placeholder='Email' require />
            <input class="text-input" type='password' name='password' placeholder='Password' require />
            <input class="submit-btn" type='submit' value="LOGIN" />
        </form>
    </div>
</body>

</html>